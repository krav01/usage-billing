version: "2"
run:
  timeout: 5m
linters:
  default: none
  enable:
    - bodyclose
    - errcheck
    - errorlint
    - govet
    - ineffassign
    - nilerr
    - staticcheck
    - unused
formatters:
  enable:
    - gofmt
    - goimports
